;(function($) {

	$.extend($, {
	  
	  email: function(options) {
      
      VERSION = 0.1.0
      self = this
      
      defaults = {
        key: 'value'
      }
      
      $.extend(options, defaults)
      
      return this.each( function(){
        
        // your plugin
        
      })
		},
	  
	})
})(jQuery);